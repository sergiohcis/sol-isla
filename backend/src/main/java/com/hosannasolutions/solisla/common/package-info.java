/** Shared value types and utilities used across modules (e.g. money/currency handling, paginated
 *  response envelopes) — not a dumping ground for business logic, which stays in its owning
 *  module's service/domain layer. */
package com.hosannasolutions.solisla.common;
