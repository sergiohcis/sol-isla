/** Operator-editable store settings ({@code app_settings}) — distinct from the {@code config}
 *  package, which holds this codebase's own cross-cutting infrastructure. */
package com.hosannasolutions.solisla.configuration;
