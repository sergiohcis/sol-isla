import { Component } from '@angular/core';

/** Placeholder pending the admin order workflow screens (ORDER_VIEW/ORDER_UPDATE_STATUS). */
@Component({
  selector: 'app-admin-orders',
  template: `<p>Admin order workflow coming soon.</p>`,
})
export class Orders {
}
