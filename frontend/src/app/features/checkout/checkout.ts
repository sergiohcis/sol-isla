import { Component } from '@angular/core';

/** Placeholder pending the checkout module — see CheckoutService's orchestration contract
 *  documented in the backend's checkout/package-info.java. */
@Component({
  selector: 'app-checkout',
  template: `<p>Checkout coming soon.</p>`,
})
export class Checkout {
}
