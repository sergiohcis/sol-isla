import { Component } from '@angular/core';

/** Public product catalog / storefront home. Placeholder pending the catalog module
 *  (CLAUDE.md implementation sequence: DB migrations -> domain models -> catalog API -> ...). */
@Component({
  selector: 'app-catalog',
  template: `<p>Catalog coming soon.</p>`,
})
export class Catalog {
}
